# stop-ai-repetitive-code-scans
This example demonstrates the inefficiency of AI agents repeatedly scanning codebases as plain text (like `grep`). It contrasts a 'Naive AI Agent' that performs full file scans for every query with a 'CodeGraph AI Agent' that builds an initial index and then answers subsequent queries instantly without re-scanning, showcasing how to optimize resour
